import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.digischemelp.app',
  appName: 'Digi-Scheme',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
